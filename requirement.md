Objective
- create tool for assessment security on AWS resources.
- AWS resource scope are EC2, VPC, IAM ,S3
- use aws cli to export resource to json and convert to cloudformation template and then Use checkov tool scan it and show report as csv format 
- must show both pass and not pass
- this tools will run on AWS Cloud shell directly , plase make it to compatible it.
